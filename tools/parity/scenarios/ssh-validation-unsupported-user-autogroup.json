{
  "name": "ssh-validation-unsupported-user-autogroup",
  "expect_policy_error": "autogroup \"autogroup:invalid\" is not supported for SSH user",
  "policy": {
    "ssh": [
      {
        "action": "accept",
        "src": ["alice@"],
        "dst": ["autogroup:self"],
        "users": ["autogroup:invalid"]
      }
    ]
  }
}
