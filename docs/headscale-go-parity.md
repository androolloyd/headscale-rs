# Headscale-go Parity Ledger

Baseline: `github.com/juanfont/headscale v0.28.0`, pinned by
`tools/parity/headscale-go/go.mod`.

Do not mix the `v0.29.0-beta.1` command/config/auth changes into this
ledger until stable v0.28 parity lands or the baseline is deliberately
bumped. Treat that beta as a separate upgrade track.

Current-head audit overlay, refreshed 2026-05-23:

- Current upstream `juanfont/headscale` HEAD used for the live parity
  audit is `4483fd0cad38717913e7509fc50f9d48c691b02b`.
- The checked-in differential harness remains pinned to v0.28.0 until
  `tools/parity/headscale-go` is deliberately rebased. Treat rows below
  as v0.28 executable coverage plus explicit current-head deltas.
- Integrated current-head fixes: `/key?v=` capability-version behavior
  and Noise placeholder route/body-limit parity in `c002804`, `/verify`
  4 KiB request-body limit in `72bb5f8`, and DB user/preauth delete
  semantics in `d076c8e`; CLI `auth` plus `preauthkeys delete`
  command parity in `56d4de6`, and current-upstream top-level
  `auth` visibility plus auth help snapshots; unsupported client capability
  rejection and Noise register error-envelope parity in `004098d`;
  map stream compression/framing negotiation in `97e8bbd`; removed
  upstream config-key rejection in `222f80a`; persistent IPv6
  projection plus IPv4 `BackfillNodeIPs` in `118a03c`; public
  `/machine/*` hard-gating in `cfa44f1`; semantic ACL policy tests
  for `CheckPolicy`/`SetPolicy` in `c546f5b`; rich
  `Hostinfo`/`NetInfo` runtime preservation in `db7ff65`; public
  `HEAD /machine/ping-response` callback routing in `47b9fad`;
  non-empty `sshTests` semantic evaluation in the current policy
  checker; and `PingRequest` map-stream dispatch/callback correlation
  plus optional dual-stack live wire address projection, DB-seeded
  production address allocation, configured prefix-family backfill
  add/remove coverage, IPv4-disabled wire/admin/DNS projection, and register-time
  debug/CLI address allocation plus duplicate admin address rejection,
  upstream IPv6-only prefix config, paired dual-stack/IPv4-only/
  IPv6-only stock-client coverage, and paired production-process
  IPv4-to-dual-stack, dual-stack-to-IPv4-only, and
  dual-stack-to-IPv6-only backfill stock-client coverage, and
  production online/LastSeen lifecycle projection, upstream DNS
  `override_local_dns` resolver/fallback behavior plus initial
  `extra_records_path` validation in `8031754`, upstream
  `taildrop.enabled` capability gating in `e625727`, upstream
  default log format `text` in `c6c583b`, and current-upstream
  `node.expiry` parsing/env/debug projection plus non-tagged
  registration default expiry, public `grants`/`nodeAttrs`/`ipsets`
  parsing, user CRUD policy/map refresh wakes, per-viewer
  `grants[].via` route steering, current-upstream config-schema
  parsing/debug projection, SQLite import fixture matrix coverage,
  DNS edge resolver/MagicDNS coverage, production DNS
  `extra_records_path` hot-reload stock-client coverage, production
  restart-persistence smoke wiring, route-withdraw approval
  preservation, paired route-via policy reload/restart and multi-prefix
  restart stock-client coverage, route-health policy reload stock-client coverage,
  production route-health restart stock-client coverage,
  expanded SSH real-client coverage, current-upstream SSH
  `localpart:*@domain` login-user compiler and `sshTests` semantics,
  paired current-head SSH localpart/profile real-client coverage,
  scoped grpc-gateway fallback routing, upstream-shaped grpc-gateway
  timestamp query parser errors plus duplicate scalar/timestamp query
  edge coverage, and
  private-DERP/STUN sidecar real-client parity in the current branch.
- Active P0 implementation lanes against the pinned v0.28 executable
  baseline: none known from the 2026-05-23 audit; current-head parity
  still has P1 upgrade-track gaps.
- Remaining P1 current-head gaps from the audit: broader paired
  stock-client route-via/route-health edge matrices, broader SSH
  client-facing status/stderr and profile-variant edges beyond the
  covered `localpart:*@domain` compiler/checker/real-client semantics,
  ACME issuance/challenge serving behavior,
  broader config reload/SIGHUP behavior, Postgres runtime support,
  native DERP relay decision beyond the supported sidecar boundary, and remaining production backfill/restart
  edges beyond the covered restart-persistence row.
- Remaining P2 current-head gaps from the audit: operator-facing ping
  initiation surfaces, exact CLI/help/error snapshots, broader gateway
  auth error text, and Octra-side documentation cleanup for the
  now-direct headscale-rs API coupling.

This ledger tracks observable behavior that must match before
`headscale-rs` can claim full parity with headscale-go. Rows marked
`Done` have differential tests or direct Rust tests in this repo.
Rows marked `Partial` have some implementation or tests but do not
cover the whole upstream surface. Rows marked `Missing` need product
implementation and parity tests.

## Executable Parity

The default differential gate is:

```sh
./scripts/headscale_go_diff.sh
```

It compares `tools/parity/headscale-rs` with headscale-go v0.28.0
over every checked-in `tools/parity/scenarios/*.json` scenario, then checks the
normalized pinned golden at `tools/parity/golden/headscale-go-v0.28.0.json`.
Current-head-only scenarios such as `grants[].via` route steering live under
`tools/parity/current-head/` and run through the Rust golden gate until the Go
harness baseline is intentionally rebased:

```sh
./scripts/headscale_rs_current_head_golden.sh
```

The stock-client parity matrix is:

```sh
tools/real-client/smoke-matrix.sh --list
```

Run the full paired matrix when Docker-backed real-client jobs are
available:

```sh
REAL_CLIENT_SMOKES=all REAL_CLIENT_TARGETS='rust headscale-go' tools/real-client/smoke-matrix.sh
```

Current executable coverage:

| Surface | Status | Evidence |
| --- | --- | --- |
| ACL literal prefixes and ports | Done | `literal-prefix-*` scenarios |
| ACL protocol names and numbers | Done | `acl-comment-fields-and-protocols`, `literal-prefix-proto-number-tcp` |
| ACL users, groups, tags, hosts, autogroups | Done | `acl-principal-resolution-matrix`, `host-alias-tcp22` |
| ACL parser and validator errors | Done | `acl-validation-*` scenarios for autogroups, unknown fields, missing references, protocols, and ports |
| Policy missing-reference validation | Done | `acl-validation-undefined-*`, `ssh-validation-undefined-*`, `tagowners-validation-undefined-group`, `tagowners-validation-undefined-tag-owner`, `autoapprovers-validation-undefined-tag-route` |
| Per-node `autogroup:self` filter reduction | Done | `acl-autogroup-self-per-node` |
| Peer map / matcher visibility | Done | `peer-map-symmetric-one-way`, `peer-map-route-visibility`, and `acl-autogroup-self-per-node` `peer_map_checks` cover `PolicyManager.BuildPeerMap` symmetric one-way ACL visibility, active approved route visibility, unapproved route exclusion, and per-node `autogroup:self` peer maps |
| `tagOwners` authorization | Done | `tag-owners-matrix`, `tagowners-nested-tag-owner`, `tagowners-validation-circular-tag-owner` |
| `autoApprovers` route and exit-node checks | Done | `route-autoapprove*` scenarios, including default-route, IP-family, prefix-containment, current-approved preservation, and tagged-node identity edge cases |
| SSH policy compilation and validation | Done | `ssh-policy-matrix`, `ssh-validation-*` scenarios |
| Tailcfg DNS, DERP, register, map, SSHPolicy JSON summaries | Partial | `wire-*` scenarios, including `RegisterRequest` capability version, old-node-key rotation, network-lock key, tailnet/follow-up/ephemeral/expiry fields, register node-key/device signature fields, `RegisterResponse.NodeKeySignature`, upstream `RegisterResponse.User`/`Login` profile fields, extended `Hostinfo`/`NetInfo` client metadata including routable IPs, services, Tailscale SSH host keys, location, TPM metadata, and DERP latency, `MapRequest` session/debug/connection-handle/hardware-attestation fields, broad `MapNode` metadata/CapMap/Cap/lifecycle timestamps including key-expiry/expired state, `MapResponse` peer/filter deltas, health/display/debug/control fields, `UserProfiles`, `SSHPolicy`, extended `DNSConfig` fields (`Nameservers`, `CertDomains`, resolver bootstrap addresses, `TempCorpIssue13969`), and extended `DERPMap` fields (`HomeParams`, region coordinates/selection flags, node cert/STUN/port-80 metadata); runtime map node names/domains now derive from the configured DNS base domain instead of an Octra-specific core default, streaming `/map` registry wakes now emit incremental `PeersChanged`/`PeersRemoved` updates, self key-expiry wakes now emit an updated self `Node`, route, disco-key, and SSH host-key updates that change full peer state emit full `PeersChanged` entries, endpoint/DERP-only updates emit `PeersChangedPatch` entries, `/map` emits headscale-go-style `PacketFilters["base"]` reduced for the receiving node, `/map` stamps requester capability version into self and peer `MapNode.Cap`, self/peer `MapNode.CapMap` now seeds upstream default admin, SSH, and file-sharing capabilities before applying policy node attrs, and full map responses stamp upstream builder fields such as `ControlTime`, `CollectServices=false`, and `Debug.DisableLogTail=true` |
| Public control HTTP basics | Partial | `tailscale_wire::basic_handlers::tests::*` covers headscale-go-compatible `/robots.txt`, `/health`, `/version`, `/windows`, `/apple`, `/apple/{platform}`, `/swagger`, `/swagger/v1/openapiv2.json`, `/verify`, `/register/{registration_id}`, `/favicon.ico`, and blank fallback response shapes on the unauthenticated control listener; Apple/Windows profile URLs prefer `WireState.public_control_url` to mirror headscale-go `cfg.ServerURL`, with request-derived fallback until full config loading supplies it |
| API-key persistence, admin REST auth, CLI, and gRPC slice | Partial | `headscale-db::api_keys` unit tests, `headscale-api/tests/api_keys_e2e.rs`, `headscale-cli/tests/admin_client.rs`, `grpc::upstream_tests::*`, and `headscale-api/tests/grpc_gateway_e2e.rs` cover headscale-go key shape, Go-compatible `api_keys.hash`/datetime row layout, bcrypt-secret storage, legacy rows, prefix parsing, validate/expire/delete/list, bearer auth, CLI HTTP wiring, the four upstream `HeadscaleService` API-key RPCs, opt-in gRPC bearer API-key auth, grpc-gateway `/api/v1/apikey` create/list/expire/delete paths, and a tonic service mount helper. Production `headscale server` now mounts the authenticated grpc-gateway plus unauthenticated local Unix-socket gRPC, protected by filesystem permissions, over the same persistent admin stores as the wire runtime |
| Pre-auth-key persistence, admin REST/CLI, and gRPC slice | Partial | `headscale-db::preauth_keys` unit tests, `headscale-api/tests/preauth_e2e.rs`, `headscale-cli/tests/admin_client.rs`, `grpc::upstream_tests::*`, and `headscale-api/tests/grpc_gateway_e2e.rs` cover create/list/expire/delete, headscale-go `hskey-auth-<12>-<64>` token shape, Go-compatible `pre_auth_keys.prefix/hash/used` row layout, `pre_auth_keys.user_id` FK enforcement with upstream `ON DELETE SET NULL` semantics, reusable/ephemeral/used/tags, tags-only keys, assigned-key delete FK rejection, orphaned `nodes.auth_key_id` cleanup during migration, ID-based upstream gRPC RPCs, OIDC user-id preauth minting through synthesized usernames, grpc-gateway `/api/v1/preauthkey` create/list/expire/delete paths, tag/user validation, `headscale preauthkeys create/list/expire` over upstream gRPC connection modes with prefix-to-ID resolution, and persistent wire auth-key registration preserving `nodes.auth_key_id`; exact upstream output/error text remains open |
| Node gRPC slice | Partial | `proto/node.proto` now matches the upstream `headscale.v1.Node` message/RPC schema instead of the previous Octra mesh schema, and `grpc::upstream_tests::*` covers `ListNodes`, `GetNode`, `SetTags`, `SetApprovedRoutes`, `BackfillNodeIPs`, `DebugCreateNode`, `RegisterNode` from the debug registration cache, `RegisterNode` consuming the same pending web-registration cache populated by wire register, blocking follow-up wire registration until CLI approval, `RenameNode`, `ExpireNode`, `ExpireNode.disable_expiry`, and `DeleteNode`; `headscale-api/tests/grpc_gateway_e2e.rs` covers the matching grpc-gateway `/api/v1/debug/node`, `/api/v1/node`, `/api/v1/node/register`, `/api/v1/node/backfillips`, and node ID subpaths with protojson-style node bodies, including `disableExpiry`/`disable_expiry` query handling and the upstream mutual-exclusion error when expiry and disable-expiry are both set. `SetTags` now rejects empty lists, malformed tags, and tags missing from the loaded policy like upstream. `headscale-db::headscale_nodes` covers the Go-shaped `nodes` row layout/core node mutations, auth-path rekey/update by machine key and user, and `PersistentMachineAdmin` now lets admin/gRPC/OIDC-adapter node operations expose numeric `nodes.id` values from SQLite. `MachineAdmin::complete_registration` now gives `RegisterNode` the same persistent web/CLI approval path as auth-key creation/update: the persistent admin writes or rekeys Go-shaped `nodes`, `WireMachineAdmin` mirrors the in-memory rekey behavior, and `HeadscaleAdminService::with_wire_registry` can project approved nodes into a live `MachineRegistry`. File-backed `RegisterNode` restart coverage now proves numeric node/user IDs, tags, routes, and pending wire metadata survive DB reopen and registry hydration. Route responses preserve headscale-go sticky primary-route ownership, keep exit routes out of `PrimaryRoutes` while surfacing them as serving routes where upstream does, persist approved routes through Go-shaped SQLite rows, and ignore expired/offline nodes when calculating serving route state. Auth-key registration can now write through persistent `nodes` and project the saved row back into the live wire registry; public grpc-gateway, local Unix-socket gRPC, optional remote TCP gRPC, and default `headscale nodes` CLI-over-gRPC are wired, while broader interactive stock-client smokes, CLI output/error snapshots, CLI config-file merging, debug/health top-level CLI wiring, and restart/runtime mutation coverage remain open |
| Policy and health gRPC slice | Partial | `grpc::upstream_tests::*`, `headscale-api/tests/grpc_gateway_e2e.rs`, and `headscale-db::policies` cover upstream `GetPolicy`, `SetPolicy`, `CheckPolicy`, and `Health` RPCs backed by `PolicyStore`, preserving raw HUJSON and update timestamps across both tonic and grpc-gateway `/api/v1/policy`, `/api/v1/policy/check`, and `/api/v1/health`; `CheckPolicy` validates candidate policy text without mutating the live store and now evaluates upstream `tests` accept/deny assertions plus `sshTests` accept/deny/check assertions against live nodes before success, including current-upstream `localpart:*@domain` SSH login-user semantics, with `SetPolicy` using the same semantic gate before persistence. `headscale policy get/set/check` now uses the upstream gRPC connection modes by default, with explicit `--server` preserving the legacy HTTP/local-check behavior. Health now pings a wired database health probe/SQLite pool and surfaces headscale-go-style ping failures through gRPC and grpc-gateway. Production `headscale server` mounts bearer-auth `/api/v1/health` on the public listener and unauthenticated local Unix-socket `Health` protected by filesystem permissions with the same SQLite health probe as the wire runtime. Config-driven DB-mode policy persistence now appends Go-shaped `policies` rows, reads the latest non-deleted row, and errors on missing policy rows like upstream; config-driven file mode loads raw policy files at startup and rejects `SetPolicy` before parsing. Broader SSH runtime/client parity remains open |
| User admin and gRPC slice | Partial | Admin REST/CLI user CRUD plus `grpc::upstream_tests::*` cover upstream `CreateUser`, `RenameUser`, `DeleteUser`, and `ListUsers` RPCs with numeric IDs and profile fields; `headscale-api/tests/grpc_gateway_e2e.rs` covers the upstream grpc-gateway singular `/api/v1/user` create/list/rename/delete paths with bearer API-key auth and protojson-style uint64/status response shapes. `headscale users create/list/rename/destroy` now defaults to the upstream local Unix-socket gRPC service, supports remote TLS `--address` + `--api-key`, implements `--insecure` as TLS certificate-verification skip instead of plaintext downgrade, supports upstream user profile/filter/rename/destroy flags plus `--force`, keeps `delete` as the upstream destroy alias, and keeps the legacy HTTP transport only when `--server` is supplied without an explicit gRPC endpoint. `headscale-db::users` now covers the headscale-go `users` row shape, provider/profile fields, uniqueness indexes, OIDC rename rejection, and DNS-hostname validation. `UserAdmin` now has both in-memory and SQLite-backed adapters, name-based persistent lookups resolve headscale-go `Username()` fallbacks for OIDC users, and admin/gRPC tests exercise the persistent Go-shaped user store; exact structured output snapshots, exact upstream error text, user/node FK enforcement, and broader web/OIDC user lifecycle coverage remain open |
| OIDC config and runtime foundation | Partial | `headscale-core::config` and `headscale-cli::config` tests cover upstream OIDC fields/defaults, TOML/YAML parsing, `HEADSCALE_OIDC_*` overrides, `client_secret_path` resolution, upstream-fatal removed keys such as `oidc.expiry`/`oidc.strip_email_domain`/`oidc.map_legacy_users`, PKCE validation, and server `server_url`/state/TLS/local Unix/remote TCP gRPC socket fields needed by the wire runtime; `headscale-api::oidc` tests cover allowed domain/user/group checks, email verification, issuer/subject identifier cleanup, UserInfo merge, profile field mapping, flexible `email_verified` and single-string `groups` decoding, optional/no node expiry, OIDC provider discovery metadata validation, config-to-runtime construction, state/nonce auth-start cookies, PKCE challenge generation, extra auth params, auth-code redirect construction, callback token exchange with PKCE verifier, JWKS-backed ID-token validation, nonce-cookie validation, callback UserInfo fetch/merge, callback claim authorization after final claims are assembled, an optional `OidcUserStore` create/update hook for merged profile claims, callback success/expired-session responses, and a generic `OidcRegistrationHandler` hook. `tailscale_wire::router_with_oidc` and `tailscale_wire::serve::ServeConfig::oidc` can mount the OIDC provider so `/register/{registration_id}` starts the auth-code flow and `/oidc/callback` completes the in-memory wire registration cache with OIDC expiry/register method; OIDC completion now preserves pending client/default expiry when no provider expiry is supplied, lets token expiry override pending/default expiry when `use_expiry_from_token` is enabled, and clears expiry for tagged OIDC registrations. `PersistentOidcRegistrationHandler` can also write/rekey Go-shaped `nodes` rows, validate pending requested tags through shared `tagOwners` policy rules, replace the old live-registry node key on same-machine reauth, preserve disabled OIDC node expiry, and complete the same pending cache when configured. `headscale server` now uses the headscale-compatible Tailscale wire serve path for both web and OIDC registration, opens/migrates the Go-shaped SQLite DB, loads the latest persisted ACL policy, strictly hydrates existing persisted `nodes` into the live wire registry, mounts the authenticated public grpc-gateway plus local Unix-socket and optional remote TCP gRPC services from the same persistent stores, wires persistent users/preauth/nodes plus the persistent OIDC handler into the shared registration cache/live registry, and requires `server.server_url` so clients receive absolute registration and callback URLs. Paired `tools/real-client/oidc-smoke.sh` and `tools/real-client/oidc-headscale-go-smoke.sh` now prove the production OIDC callback flow against Rust and pinned headscale-go with stock Tailscale clients; persisted policy/route mutation and CLI-over-gRPC serving coverage remain open |

`ipsets` are intentionally not in the differential harness because the
pinned headscale-go v0.28 policy struct does not expose them. Rust
supports `ipsets` as an extension; add differential coverage only after
the Go baseline is bumped to a version that exposes the same surface.

## Full Parity Matrix

| Area | Status | Required work |
| --- | --- | --- |
| Config loading | Partial | Match `config.yaml` search paths, env overrides, validation, TLS/ACME, metrics, DNS, DERP, OIDC, database, gRPC, and CLI config fields. Rust now covers the major v0.28 config/runtime surfaces: DNS defaults and validation, Taildrop, log format, OIDC, top-level server/listener/gRPC/noise/prefix/database/TLS/DERP/policy/CLI aliases, default config discovery, pinned v0.28 config-example-derived fixture parsing, loaded `/debug/config`, manual TLS on the public and remote gRPC listeners, config-backed route HA probe timing, trusted-proxy forwarding-header gating, SIGHUP ACL policy reload with auto-approval fan-out, and current-upstream parse/projection for update-check, logtail, auto-update, tuning, and database subfields where runtime supports them. ACME issuance/challenge listeners are still unsupported but now fail `configtest`/server with challenge-specific HTTP-01 vs TLS-ALPN-01 context plus the configured ACME cache directory, unsupported Postgres configs now fail before SQLite startup with an explicit SQLite-only message, and byte-for-byte `configtest` fatal text remains open. |
| Database models | Partial | Match users, nodes, preauth keys, API keys, routes, policies, migrations, soft-delete behavior, timestamps, expiry, and uniqueness constraints; API-key, pre-auth-key, user, policy history, and canonical node row shapes now track headscale-go, legacy Octra mesh nodes are isolated in `octra_nodes`, admin/gRPC users and nodes can use Go-compatible SQLite stores, preauth rows enforce the upstream `users(id)` FK while migrating matching legacy username labels to numeric IDs, approved routes persist in `nodes.approved_routes`, advertised-route updates can mutate `host_info.RoutableIPs` without clearing approvals, map-time runtime state can persist `disco_key`, `endpoints`, `Hostinfo` identity/routes/SSH keys/PreferredDERP, legacy Go `routes` rows migrate enabled approvals into `nodes.approved_routes`, and SQLite import tests now cover modern bcrypt-prefixed preauth/API-key rows plus linked auth-key node metadata across v0.26.0, v0.27.1, v0.28.0-beta.1, v0.28.0-beta.2, and v0.28.0 fixture histories. The DB guard accepts the pinned v0.28-compatible SQLite window, rejects unsupported older/future Go migration markers, and documents Postgres as unsupported in Rust DB import/runtime today. The live wire runtime is still not fully DB-backed. |
| Node registration | Partial | Match preauth-key registration, reusable/ephemeral/expired keys, CLI debug registration, web registration, OIDC registration, machine-key reuse, node-key expiry, forced tags, and duplicate handling; pre-auth key token/storage format plus user/node storage shapes now match upstream, CLI debug registration can persist through the Go-shaped node adapter, unauthenticated wire registration now creates a Go-shaped 24-byte web registration ID/AuthURL in a shared pending cache, the public web registration page validates that ID and renders the upstream CLI instruction, gRPC `RegisterNode` consumes and completes the same pending entry, wire follow-up requests block until approval, expired follow-ups restart with a fresh AuthURL, and the cache reports headscale-go's 15m/20m expiry/cleanup tuning. Existing registered nodes now handle no-auth restart registration without re-entering web registration, reject client-side expiry extension, process past-expiry logout for both persistent and ephemeral nodes, persist the TS2021 Noise machine key into auth-key and pending web registrations, reject mismatched machine keys on no-auth existing-node requests, auth-key same-user reauth/node-key rotation updates the existing machine-key identity instead of duplicating the node while preserving its address, created timestamp, endpoints, and still-advertised approved routes, and web/CLI same-user reauth now rekeys the existing machine-key identity and clears tags when the client no longer requests any. Used/expired preauth-key re-registration is accepted only for the exact same machine/user/node-key identity, tagged preauth-key registration disables requested/default node-key expiry while untagged preauth preserves future client expiry or falls back to configured `node.expiry`, Go zero-time client expiry falls back to the default, `node.expiry = 0` disables the default, and same-machine re-registration refreshes the default expiry. Client-requested `Hostinfo.RequestTags` are rejected on preauth-key registration, pending web/CLI registrations carry request tags into gRPC approval with `tagOwners` validation, carry configured default expiry when no future client expiry is supplied and no tags are requested, and disable node-key expiry for tagged approvals, and assigned preauth-key deletion is blocked like upstream after orphaned node FK cleanup. Auth-key wire registration also normalizes advertised routes, persists policy auto-approved routes, and can write through to Go-shaped `nodes.auth_key_id` when the persistent server runtime is active. Persistent auth-key, OIDC handler, and web/CLI approval paths now preserve pending wire metadata such as disco key, endpoints, PreferredDERP, SSH host keys, routes, tags, and auth/user FKs through Go-shaped `nodes` and fresh registry hydration. Persistent web/CLI approval now uses the shared `MachineAdmin::complete_registration` path so `RegisterNode` can create or rekey Go-shaped `nodes` and, when configured, project the approval into the live wire registry. Public grpc-gateway plus local Unix-socket and optional remote TCP gRPC production mounting are wired; broader interactive restart/runtime mutation coverage remains open. |
| Poll/map runtime | Partial | Match `/machine/{key}/map`, streaming updates, keep-alives, full vs incremental map responses, peer changes, endpoint updates, disco key updates, hostinfo updates, logout, and expiry behavior; map node FQDNs now follow the configured DNS base domain, loaded policies now reduce visible peers using symmetric node/route access checks, full map responses now carry upstream builder control/debug/service fields and de-duplicated `UserProfiles`, `/map` self and peer nodes now carry the requester `MapRequest.Version` in `MapNode.Cap`, expired self nodes now use upstream `Node.KeyExpiry`/`Expired`/`MachineAuthorized` state instead of a non-upstream map-level expiry bit, streaming registry wakes now use `PeersChanged`/`PeersRemoved` deltas for peer add/remove/update events, self key-expiry updates emit the updated self `Node`, full `PeersChanged` deltas for route updates that change peer visibility/route `AllowedIPs`, subnet-router online/offline transitions, and disco-key-only updates, and `PeersChangedPatch` for endpoint-only, preferred-DERP, and non-router online/offline peer updates. Online state is now volatile stream-session state with a 10-second disconnect grace, and full `MapNode.LastSeen` is suppressed while a node is online like headscale-go. Map/register HostInfo updates now preserve the full client `Hostinfo`/`NetInfo` snapshot while maintaining hostname/OS/route/SSH-key/DERP projections, omitted `NetInfo` preserves the previous value, map HostInfo route updates now persist newly policy auto-approved routes while preserving existing approvals, DB-backed runtime sync now writes map-time last-seen, disco key, endpoints, full `Hostinfo` identity/routes/SSH keys/NetInfo into Go-shaped `nodes` and hydrates those fields back into the live registry after restart, and register-time logout requests with past expiry expire persistent nodes or remove ephemeral nodes like headscale-go. Non-streaming `OmitPeers` endpoint-update requests now match headscale-go's lite 200-empty response while still updating node state, leaving downstream Octra domains to Octra-side wiring. |
| Tailcfg wire model | Partial | `RegisterRequest` version/old-node-key/NLKey/tailnet/follow-up/ephemeral/expiry/signature fields, `RegisterResponse.NodeKeySignature` and upstream User/Login/Error shape, expanded `HostInfo` client metadata including legacy services, geolocation, TPM, and `NetInfo` preferred-DERP/link/firewall/DERP-latency fields, `DNSConfig` legacy/temp/cert/resolver fields, `DERPMap` home-parameter/region/node metadata fields, `MapRequest` session/keepalive/debug/connection-handle/hardware-attestation fields, broad `MapNode` metadata including tags/routes/HomeDERP/Cap/capabilities/CapMap, default admin/SSH/file-sharing CapMap entries, lifecycle timestamps, key-expiry/expired state, and WireGuard-only metadata, nodeAttrs-to-CapMap emission, top-level `randomizeClientPort` emission as `randomize-client-port`, policy-driven `MapResponse.Peers` reduction for node IPs and served routes, per-node `PacketFilters["base"]` emission for node IPs and served routes, `MapResponse.UserProfiles`, tagged-node `TaggedDevices` user identity, peer deltas, packet-filter deltas, health/display/debug/control messages, and `SSHPolicy` are covered at the JSON/runtime-test layer; upstream-shaped `taildrop.enabled` now gates the file-sharing CapMap entry and policy node attributes cannot re-enable it while disabled. Still wire remaining route state and client capability negotiation edges. |
| Policy parsing and validation | Partial | SSH parser/error parity plus ACL autogroup placement, unknown fields, missing group/tag/host references, protocol/port errors, nodeAttrs-to-CapMap, top-level `randomizeClientPort`, upstream-shaped `tests`/`sshTests` parsing, ACL `tests` shape validation, `sshTests` semantic evaluation, current-upstream `localpart:*@domain` SSH login-user semantics, and policy update cache invalidation are covered. The public HuJSON parser now accepts and validates upstream-main `grants`, `nodeAttrs`, and `ipsets`, projects network `grants[].ip` into ACL/filter behavior, keeps empty ACL/grants policies deny-all, validates app capability and `via` tag constraints, and uses `grants[].via` to steer route `AllowedIPs` per viewer. Still add broader differential policy v2 fixtures and SSH profile/error variants once the executable baseline moves past v0.28. |
| Peer map/matchers | Partial | Exact differential `peer_map_checks` now compare Rust against headscale-go `PolicyManager.BuildPeerMap` for symmetric one-way ACL visibility, active approved route visibility, unapproved route exclusion, and per-node `autogroup:self` peer maps; `headscale-api/tests/policy_consolidation.rs` covers the Rust helper directly, and `tailscale_wire::map::tests::map_response_*policy*` verifies loaded policies reduce `/map` peers while preserving route-visible routers. Runtime `/map` now emits per-node reduced `PacketFilters["base"]` for node IPs and served routes, including the headscale-go empty-ACL streaming edge where full maps remain empty but later registry deltas can surface newly joined peers without loosening packet filters. `tools/real-client/acl-allow-smoke.sh` / `tools/real-client/acl-allow-headscale-go-smoke.sh`, `tools/real-client/acl-empty-smoke.sh` / `tools/real-client/acl-empty-headscale-go-smoke.sh`, and `tools/real-client/acl-autogroup-self-smoke.sh` / `tools/real-client/acl-autogroup-self-headscale-go-smoke.sh` now run matching stock-client loaded-ACL, empty-ACL, and multi-user `autogroup:self` peer visibility checks against Rust and pinned headscale-go. Remaining `MatchersForNode` cache edges still need broader integration coverage. |
| Tailscale SSH | Partial | Differential compiler and validation coverage exists, `MapResponse.SSHPolicy` is emitted, `Hostinfo.sshHostKeys` round-trips into peer `MapNode` entries, current-upstream `localpart:*@domain` login-user compiler and `sshTests` semantics are covered, paired `tools/real-client/ssh-smoke.sh` / `ssh-headscale-go-smoke.sh` stock-client scripts cover same-user `autogroup:self` allow, cross-user policy denial, ACL-blocked packet-filter timeout, flat multi-group source matrices, no-policy denial, and `check`/check-period behavior, and paired `tools/real-client/ssh-localpart-smoke.sh` / `ssh-localpart-headscale-go-smoke.sh` cover current-head localpart login users from profile emails. Remaining upstream SSH edges are broader profile variants and exact client-facing status/stderr text. |
| DNS/MagicDNS | Partial | Match MagicDNS names, search domains, split DNS, resolvers, extra records, collisions, machine records, IPv4/IPv6, disabled DNS, and hot reload behavior; default/base-domain/override-local-DNS validation now follows headscale-go while downstream domains remain embedder-supplied, production `headscale server` now injects configured DNS into the live map runtime, including MagicDNS/base domain, search domains, global and split nameservers, `override_local_dns` resolver-vs-fallback behavior, inline extra records, and the extra-records file watcher with upstream-style initial file validation, and `wire-dns-derp-extended` differentially covers legacy `Nameservers`, `CertDomains`, resolver bootstrap addresses, empty split-DNS routes, `TempCorpIssue13969`, and additional A/AAAA/CNAME-style extra records. `tools/real-client/magicdns-smoke.sh` / `tools/real-client/magicdns-headscale-go-smoke.sh` now run matching two-client stock-client logins against Rust and pinned headscale-go and assert the client-observed MagicDNS suffix plus self/peer DNS names. `tools/real-client/magicdns-custom-domain-smoke.sh` / `tools/real-client/magicdns-custom-domain-headscale-go-smoke.sh` run the same stock-client assertions with a non-default `base_domain`. `tools/real-client/dns-disabled-smoke.sh` / `tools/real-client/dns-disabled-headscale-go-smoke.sh` run the same stock-client pair with MagicDNS disabled and assert the headscale-go-compatible bare-hostname DNS status fallback. `tools/real-client/dns-edge-*` and `tools/real-client/magicdns-ipv6-only-*` now add paired coverage for split resolver routes, structured global/fallback resolver metadata, AAAA/CNAME extra records, MagicDNS normalization/collision caps, IPv6-only prefix MagicDNS records, and map-stream DNS hot-reload behavior at the Rust layer. `tools/real-client/dns-hot-reload-smoke.sh` / `tools/real-client/dns-hot-reload-headscale-go-smoke.sh` now add paired production-process `extra_records_path` reload coverage observed by a stock client. Remaining edges include upstream-vs-Rust MagicDNS-generated `ExtraRecords` JSON differences, `CertDomains`/HTTPS behavior, and broader live resolver client behavior. |
| Routes | Partial | Advertised and approved route storage now feeds node gRPC responses and map response `AllowedIPs`/primary routes, with stateful headscale-go-style sticky primary promotion/failover, exit routes separated from subnet `PrimaryRoutes`, expired/offline nodes excluded from serving-route calculations, registration/map paths applying policy `autoApprovers`, admin/gRPC policy updates re-running auto-approval across existing nodes without removing existing approvals, admin REST/CLI route listing plus approval updates writing the same approved-route state, persistent gRPC/grpc-gateway approval coverage through Go-shaped SQLite rows, legacy `routes` table approvals migrated into node approval state, and streaming map clients receiving full `PeersChanged` deltas when a peer's advertised route update changes visibility or route-derived `AllowedIPs`; `route-autoapprove-edge-matrix` differentially covers upstream default-route, IP-family, prefix-containment, current-approved preservation, and tagged-node identity auto-approval edge cases. `tools/real-client/route-advertise-smoke.sh` / `tools/real-client/route-advertise-headscale-go-smoke.sh`, `tools/real-client/route-approve-smoke.sh` / `tools/real-client/route-approve-headscale-go-smoke.sh`, `tools/real-client/route-primary-smoke.sh` / `tools/real-client/route-primary-headscale-go-smoke.sh`, `tools/real-client/route-primary-failover-smoke.sh` / `tools/real-client/route-primary-failover-headscale-go-smoke.sh`, `tools/real-client/route-primary-sticky-smoke.sh` / `tools/real-client/route-primary-sticky-headscale-go-smoke.sh`, `tools/real-client/route-primary-withdraw-smoke.sh` / `tools/real-client/route-primary-withdraw-headscale-go-smoke.sh`, `tools/real-client/route-exit-node-smoke.sh` / `tools/real-client/route-exit-node-headscale-go-smoke.sh`, `tools/real-client/route-via-smoke.sh` / `tools/real-client/route-via-headscale-go-smoke.sh`, `tools/real-client/route-via-reload-smoke.sh` / `tools/real-client/route-via-reload-headscale-go-smoke.sh`, `tools/real-client/route-via-restart-smoke.sh` / `tools/real-client/route-via-restart-headscale-go-smoke.sh`, `tools/real-client/route-via-multiprefix-smoke.sh` / `tools/real-client/route-via-multiprefix-headscale-go-smoke.sh`, `tools/real-client/route-health-smoke.sh` / `tools/real-client/route-health-headscale-go-smoke.sh`, `tools/real-client/route-health-reload-smoke.sh` / `tools/real-client/route-health-reload-headscale-go-smoke.sh`, `tools/real-client/route-health-restart-smoke.sh` / `tools/real-client/route-health-restart-headscale-go-smoke.sh`, `tools/real-client/route-health-all-unhealthy-smoke.sh` / `tools/real-client/route-health-all-unhealthy-headscale-go-smoke.sh`, and `tools/real-client/route-health-mixed-exit-smoke.sh` / `tools/real-client/route-health-mixed-exit-headscale-go-smoke.sh` now run matching stock-client advertised-route, approved-route, primary-route, primary failover, sticky primary return, primary withdrawal, exit-node, current-head `grants[].via`, current-head policy-reloaded `grants[].via`, current-head production-restarted `grants[].via`, current-head multi-prefix `grants[].via`, current-head route-health failover, current-head policy-reloaded route-health failover, current-head production-restarted route-health failover, current-head all-unavailable route-health degraded-primary retention, and current-head mixed exit-node/subnet-router route-health logins against Rust and headscale-go and assert route state. Recent route-edge coverage preserves approvals when routes are temporarily unadvertised, supports explicit unapproval, preserves approvals across policy reload, handles simultaneous/offline/expired/ephemeral router candidates, asserts policy-file reload recomputes `grants[].via` route ownership, and asserts withdraw map-delta shape. Remaining route gaps are broader route-via/route-health edge matrices such as additional paired stock-client policy/restart combinations and broader config reload. |
| Tags | Partial | Match tag ownership, forced tags, invalid tag handling, registration tags, tag removal, and tagged-node user semantics across policy, API, CLI, and map/register responses; nested tagOwners now flatten tag-owner chains and reject circular or undefined tag-owner references like headscale-go, preauth-key tags now stamp initial wire registrations, client-advertised `Hostinfo.RequestTags` round-trip on the tailcfg wire model, preauth-key registration rejects request tags, web/CLI registration validates request tags against shared `tagOwners` ownership before applying them, web/CLI same-machine reauth with no requested tags clears the forced tag set and returns the node to the registering user, map/register JSON uses headscale-go's synthetic `TaggedDevices` identity for tagged nodes, tagged node-key expiry stays disabled on registration/reauth, and both gRPC `SetTags` and REST machine tag updates require non-empty policy-defined `tag:` values. `tools/real-client/tagged-preauth-smoke.sh` / `tools/real-client/tagged-preauth-headscale-go-smoke.sh`, `tools/real-client/tag-update-smoke.sh` / `tools/real-client/tag-update-headscale-go-smoke.sh`, `tools/real-client/tag-update-invalid-smoke.sh` / `tools/real-client/tag-update-invalid-headscale-go-smoke.sh`, `tools/real-client/tag-reauth-clear-smoke.sh` / `tools/real-client/tag-reauth-clear-headscale-go-smoke.sh`, `tools/real-client/web-register-tags-smoke.sh` / `tools/real-client/web-register-tags-headscale-go-smoke.sh`, and `tools/real-client/web-register-unowned-tag-smoke.sh` / `tools/real-client/web-register-unowned-tag-headscale-go-smoke.sh` now run matching stock-client tagged auth-key, accepted/rejected operator tag replacement, reauth-based tag clearing, accepted tagged web/CLI registration, and rejected unowned-tag web/CLI registration against Rust and pinned headscale-go. Remaining tag edges need broader API/CLI and map expiry coverage. |
| API keys | Partial | DB-backed create/list/expire/delete, headscale-go `hskey-api-<12>-<64>` shape, Go-compatible `api_keys.hash`/datetime row layout, bcrypt secret hashing, legacy `prefix.secret` validation, REST bearer-auth middleware, upstream `HeadscaleService` API-key RPCs, grpc-gateway API-key paths, opt-in gRPC bearer API-key auth, and `headscale apikeys create/list/expire/delete` over the upstream gRPC connection modes are covered, with legacy HTTP retained only for explicit `--server` use; still add broader upstream integration coverage and exact output/error snapshots. |
| Upstream gRPC API | Partial | The `User`, `Node`, `ApiKey`, `PreAuthKey`, `Policy`, and `Health` proto messages plus their implemented `headscale.v1.HeadscaleService` RPCs are generated and backed by admin state, with users/nodes/API keys/preauth keys able to use Go-compatible SQLite adapters; debug-created CLI registration, web-registration cache approval via `RegisterNode`, node route operations, numeric SQLite node IDs, auth-required service mode, tonic mount helpers, a standard gRPC reflection descriptor/service builder for the pinned v0.28 `HeadscaleService`, and grpc-gateway-backed `Health`/`User`/`PreAuthKey`/`ApiKey`/`Node`/`Policy`/`DebugCreateNode` HTTP annotation slices are covered. The upstream-main auth approval surface now has generated `auth.proto` messages and compatibility `AuthRegister`/`AuthApprove`/`AuthReject` gRPC plus gateway routes backed by the v0.28 registration cache. Production `headscale server` mounts the authenticated grpc-gateway on the public listener, unauthenticated local Unix-socket gRPC with reflection, and optional remote TCP gRPC with API-key-authenticated admin RPCs plus reflection using the same persistent users/API-key/preauth/node/policy stores as the wire runtime. The admin CLI now has a reusable gRPC client path and exercises user create/list/rename/destroy, nodes list/show/register/expire/logout/rename/tag/approve-routes/delete/backfillips, API-key create/list/expire/delete, preauth-key create/list/expire, policy get/set/check, top-level health, current-upstream auth register/approve/reject, and `debug create-node` over local Unix gRPC by default, including user profile fields, user filter selectors, node numeric identifier flags, API-key prefix/ID selectors, preauth user-name lookup, preauth prefix-to-ID resolution, policy raw-document round-trips, upstream `[cli]` config merging, and remote TLS `--insecure` coverage. grpc-gateway path `uint64` parameters now parse Go base-0 integer literals (`0x`, `0b`, `0o`, leading-zero octal, and underscores) with Go-style `strconv.ParseUint` error text. Remaining upstream-main auth-request model semantics, auth output/error exactness, grpc-gateway error text/query parser edge cases, CLI-over-gRPC for tailnet top-level commands, and broader integration tests remain open; v0.28 exposes version via public HTTP `/version`, not a `HeadscaleService` RPC. |
| REST/swagger API | Partial | Match `/api/v1`, `/swagger`, `/version`, auth, request/response shapes, status codes, and error bodies; the public wire router now covers `/robots.txt`, `/health`, `/version`, `/windows`, `/apple`, `/apple/{platform}`, `/swagger`, `/swagger/v1/openapiv2.json`, `/verify`, `/register/{registration_id}`, `/derp/probe`, `/derp/latency-check`, `/bootstrap-dns`, `/metrics`, `/debug`, `/debug/`, `/debug/vars`, `/debug/varz`, `/debug/gc`, `/debug/pprof`, `/debug/pprof/`, `/debug/pprof/cmdline`, `/debug/pprof/profile`, `/debug/pprof/symbol`, `/debug/pprof/trace`, `/debug/pprof/{profile}`, `/debug/statsviz`, `/debug/statsviz/`, `/debug/statsviz/ws`, `/debug/overview`, `/debug/config`, `/debug/routes`, `/debug/derp`, `/debug/registration-cache`, `/debug/filter`, `/debug/policy`, `/debug/ssh`, `/debug/nodestore`, `/debug/mapresponses`, `/debug/batcher`, `/debug/policy-manager`, `/favicon.ico`, and blank fallback with config-backed helper URLs where upstream uses `server_url`. `grpc_gateway::router` now covers the pinned v0.28 `headscale.v1.HeadscaleService` HTTP annotations for `/api/v1/health`, singular `/api/v1/user`, `/api/v1/preauthkey`, `/api/v1/apikey`, `/api/v1/node`, `/api/v1/debug/node`, and `/api/v1/policy` plus the upstream-main auth shim paths `/api/v1/auth/register`, `/api/v1/auth/approve`, and `/api/v1/auth/reject` with upstream-style HTTP-edge bearer auth returning plain `Unauthorized`, protojson-style success/status bodies, Go-style base-0 `uint64` path parsing, and persistent route approval through the Go-shaped node table; production `headscale server` mounts those `/api/v1` routes on the public listener. Remaining exact upstream grpc-gateway parser/error edge cases and broader gRPC/CLI integration remain open. |
| CLI parity | Partial | `headscale users create/list/rename/destroy`, `headscale nodes list/show/register/expire/logout/rename/tag/approve-routes/delete/backfillips`, `headscale apikeys create/list/expire/delete`, `headscale preauthkeys create/list/expire`, `headscale policy get/set/check`, top-level `headscale health`, and `headscale debug create-node` now use upstream-compatible gRPC by default through the local Unix socket, with remote TLS `--address`/`--api-key`, `--insecure` certificate-verification skip, user profile/filter flags, node numeric identifier flags, API-key prefix/ID selectors, preauth user-name lookup, preauth prefix-to-ID resolution, policy server-side validation, destroy/delete confirmation with global `--force`, and legacy HTTP fallback when `--server` is supplied without an explicit gRPC endpoint. Upstream command aliases cover `user`/`namespace`/`namespaces`/`ns`, embedded admin `node` plus standalone `machine`/`machines`, `preauthkey`/`authkey`/`pre`, user/preauth/API-key create/list aliases, node expire/logout/exp/e plus list/tag/delete aliases, preauth expire aliases, policy get/set aliases, top-level current-upstream `auth`, `serve` as a server alias, `generate private-key`, hidden `dumpConfig`, `version`, `completion`, `configtest`, and the env-driven `mockoidc` test provider. Admin commands now merge upstream `[cli] address/api_key/insecure` plus top-level `unix_socket` config into the gRPC connection with flag/env precedence, and configtest uses upstream default config discovery plus listener-free validation for the loaded Rust server URL, OIDC, DNS, embedded DERP, SQLite/Postgres boundary, and TLS-conflict slices. Process-level tests now cover root help command presence including `auth`, auth register/approve/reject help snapshots, `serve`, `debug create-node --help`, `generate private-key`, `mockoidc` no-config-load/env validation, machine-readable `version`, no-config-load `completion`, default config discovery, unsupported Postgres `serve` failure before SQLite startup, and missing-config validation failure. Still add upstream byte-for-byte help/output/error snapshots, broader server-init validation, and externally visible error text parity. |
| OIDC | Partial | Config/helper parity is in progress: upstream config fields/defaults, CLI TOML/YAML parsing, `HEADSCALE_OIDC_*` overrides, `client_secret_path` handling, removed-key validation for the current `oidc.expiry` to `node.expiry` move and older legacy OIDC keys, PKCE validation, provider discovery/startup metadata validation, config-to-runtime construction, claims authorization, UserInfo merging, profile field mapping, issuer/subject provider identifiers, flexible `email_verified` decoding, helper-level node-expiry selection, auth-code start routing with state/nonce cookies, PKCE, scopes, extra params, callback token exchange with PKCE verifier, JWKS-backed ID-token validation, nonce-cookie validation, UserInfo retrieval/merge, post-merge allowed domain/user/group decisions, optional callback user create/update via `OidcUserStore`, callback registration-completion hook, success/expired-session responses, opt-in serve-level OIDC route mounting into the in-memory wire registration cache, an opt-in persistent OIDC registration handler that writes/rekeys `nodes`, validates requested tags through the same shared `tagOwners` helper used by web/CLI approval, replaces the old live-registry node key, and completes the pending cache, a router-level mock OIDC callback handoff regression, and an OIDC-configured `headscale server` path that builds the wire runtime from the live SQLite/user/preauth/node stores, loads persisted ACL policy, and strictly hydrates persisted nodes into the live registry now have local coverage. Paired stock-client OIDC smokes now prove the production callback flow against Rust and pinned headscale-go; still wire persisted policy/route changes through the full serving topology. |
| Web registration | Partial | Public `/register/{registration_id}` now matches headscale-go's 24-byte registration ID validation, `invalid registration id` error body, HTML content type, and CLI registration instruction page; unauthenticated wire register creates a 24-character URL-safe registration ID/AuthURL and stores pending machine state, including requested ACL tags and configured default expiry when no future client expiry is supplied and no tags are requested, in a shared registration cache, gRPC `RegisterNode` validates those tags against `tagOwners` and approves that same pending entry for follow-up wire registration, OIDC callback can complete the same in-memory registration cache with OIDC expiry/register method, pending follow-up requests wait for approval, and expired or missing follow-ups restart the web-registration flow with a fresh AuthURL. Persistent `RegisterNode` approval now calls `MachineAdmin::complete_registration`, so web/CLI approval can create or update Go-shaped `nodes`, mirror in-memory rekey behavior through `WireMachineAdmin`, and project approvals into a live `MachineRegistry` when the admin service is built with `with_wire_registry`. `tools/real-client/web-register-smoke.sh` / `tools/real-client/web-register-headscale-go-smoke.sh`, `tools/real-client/web-register-tags-smoke.sh` / `tools/real-client/web-register-tags-headscale-go-smoke.sh`, and `tools/real-client/web-register-unowned-tag-smoke.sh` / `tools/real-client/web-register-unowned-tag-headscale-go-smoke.sh` now run matching stock-client no-auth, accepted requested-tag, and rejected unowned-tag web/CLI registrations against Rust and pinned headscale-go. Public grpc-gateway production mounting is wired; interactive persisted-node restart/runtime mutation coverage remains open. |
| Embedded DERP | Partial | Match DERP server, DERP map loading/defaults, STUN behavior, private DERP config, TLS behavior, and integration coverage; `/verify` now accepts headscale-go DERP admission requests and checks the runtime machine registry, `/derp/probe` and `/derp/latency-check` match the upstream browser latency probe responses, `/bootstrap-dns` resolves configured DERP node hostnames into the upstream JSON map shape, `/debug/derp` exposes the configured DERP map in headscale-go text/JSON shapes, `wire-dns-derp-extended` differentially covers `DERPMap.HomeParams`, region latitude/longitude/selection flags, and node cert/STUN/port-80 metadata, and `headscale server` can now build a config-backed embedded DERP map, run a native UDP STUN responder, derive private-DERP host/port metadata from `server_url`, and supervise an upstream `derper` sidecar for relay traffic. Paired `tools/real-client/derp-private-*` smokes now cover private DERP relay through the sidecar, Tailscale-style STUN probes, verify-client admission, generated DERP-map metadata, and headscale-go TLS/server-url behavior. Native Rust DERP relay protocol handling is not claimed as implemented; whether to build it instead of relying on the upstream `derper` sidecar remains an explicit product decision. |
| Noise/control protocol | Partial | Match Noise handshake, controlbase transport, TS2021 behavior, error paths, early noise response, key rotation, and client compatibility; TS2021 h2 dispatch now carries the initiator static machine key through request extensions so registration can persist and validate it. |
| State manager | Partial | Registry generation watches, policy/DNS/DERP wake-ups, streaming peer add/remove deltas, endpoint/preferred-DERP peer patches, volatile online/offline state with delayed disconnect, disco-key and route update propagation via full peer deltas, upstream-shaped per-node ephemeral GC timers, production startup scheduling for hydrated ephemeral nodes, persistent-node deletion on GC, 5-second scheduled node-expiry wakeups, live-registry write-through for persistent admin node mutations, route primary-state updates that ignore offline nodes, route-debug snapshots, and policy-update route auto-approval fan-out have local coverage; production DERP URL/path auto-update now replaces the live map on successful refresh, keeps the previous map on failure, merges embedded DERP after external sources, wakes existing `/map` streams, and updates `/debug/derp` plus `/debug/config`; production SIGHUP now reloads the configured ACL policy source and re-runs route auto-approvals, matching the v0.28 policy-reload boundary rather than full config reload. Still match headscale-go node update batching, full state-change reason sets, and remaining map/debug state. |
| Metrics/debug | Partial | `/metrics`, `/debug`, `/debug/`, `/debug/vars`, `/debug/varz`, `/debug/gc`, `/debug/pprof`, `/debug/pprof/`, `/debug/pprof/cmdline`, `/debug/pprof/profile`, `/debug/pprof/symbol`, `/debug/pprof/trace`, `/debug/pprof/{profile}`, `/debug/statsviz`, `/debug/statsviz/`, `/debug/statsviz/ws`, `/debug/overview`, `/debug/config`, `/debug/routes`, `/debug/derp`, `/debug/registration-cache`, `/debug/filter`, `/debug/policy`, `/debug/ssh`, `/debug/nodestore`, `/debug/mapresponses`, `/debug/batcher`, and `/debug/policy-manager` now cover the headscale-go debug surface for Prometheus-compatible runtime counters, debug index navigation, the tsweb Go-metrics and Prometheus aliases, pprof and statsviz route compatibility, upstream map-response metric names (`headscale_mapresponse_endpoint_updates_total`, `headscale_mapresponse_generated_total`, `headscale_mapresponse_sent_total`, `headscale_mapresponse_ended_total`, and opt-in `headscale_mapresponse_last_sent_seconds`), upstream HTTP metric names (`headscale_http_requests_total` and `headscale_http_duration_seconds`), upstream NodeStore metric names (`headscale_nodestore_operations_total`, `headscale_nodestore_operation_duration_seconds`, `headscale_nodestore_batch_size`, `headscale_nodestore_batch_duration_seconds`, `headscale_nodestore_snapshot_build_duration_seconds`, `headscale_nodestore_nodes_total`, `headscale_nodestore_peers_calculation_duration_seconds`, and `headscale_nodestore_queue_depth`), state overview, runtime config snapshots, primary routes, DERP map inspection, registration-cache metadata, current packet filters, raw policy output, per-node SSH policy output, NodeStore summaries, the disabled map-response dump state, active streaming map connections, and policy-manager inspection; `/debug/gc` is a Rust no-op compatibility endpoint because there is no GC to force, Rust runtime JSON snapshots are still not a byte-for-byte persisted `types.Node`/`types.Config` dump, and pprof/statsviz return Rust runtime metadata instead of Go pprof protobuf/websocket samples. Still match operational logs that tests depend on. |
| Integration test parity | Partial | `tools/real-client/headscale-rs-harness` now starts the headscale-rs Tailscale wire surface as a standalone stock-client target with harness-only preauth minting, pending web-registration approval/rejection, policy injection, DNS base-domain config, DERP-map loading, machine-state inspection, harness-only route approval, harness-only tag replacement/rejection, and opt-in per-client auth-key users/routes/exit-node flags plus route-health probe timing for route smokes. `tools/real-client/smoke-matrix.sh --list` is the executable source of truth for paired stock-client rows. `tools/real-client/authkey-smoke.sh` runs stock `tailscale/tailscale:v1.94.1` client containers through reusable auth-key login against that harness, waits for logged-in self nodes in the netmap, and asserts registration state. `tools/real-client/authkey-headscale-go-smoke.sh` runs the matching stock-client reusable auth-key scenarios against pinned headscale-go v0.28.0 by default and can target the audited current-head commit for upgrade-track route/SSH smokes. `tools/real-client/web-register-*`, `tools/real-client/web-register-tags-*`, `tools/real-client/web-register-unowned-tag-*`, `tools/real-client/tagged-preauth-*`, `tools/real-client/tag-update-*`, `tools/real-client/tag-reauth-clear-*`, `tools/real-client/acl-allow-*`, `tools/real-client/acl-empty-*`, `tools/real-client/acl-autogroup-self-*`, `tools/real-client/magicdns-*`, `tools/real-client/dns-disabled-*`, `tools/real-client/prefix-family-dual-stack-*`, `tools/real-client/prefix-family-v4-to-dual-backfill-*`, `tools/real-client/prefix-family-dual-stack-to-ipv4-only-backfill-*`, `tools/real-client/prefix-family-dual-stack-to-ipv6-only-backfill-*`, `tools/real-client/prefix-family-ipv4-only-*`, `tools/real-client/prefix-family-ipv6-only-*`, `tools/real-client/route-advertise-*`, `tools/real-client/route-approve-*`, `tools/real-client/route-primary-*`, `tools/real-client/route-primary-failover-*`, `tools/real-client/route-primary-sticky-*`, `tools/real-client/route-primary-withdraw-*`, `tools/real-client/route-exit-node-*`, `tools/real-client/route-via-*`, `tools/real-client/route-health-*`, `tools/real-client/oidc-*`, `tools/real-client/restart-persistence-*`, `tools/real-client/dns-edge-*`, `tools/real-client/magicdns-ipv6-only-*`, `tools/real-client/derp-private-*`, `tools/real-client/ssh-*`, and `tools/real-client/ssh-localpart-*` now exercise the same stock-client web registration, accepted/rejected tagged web registration, tagged-preauth, accepted/rejected operator tag replacement, reauth-based tag clearing, loaded-ACL peer visibility, empty-ACL streaming peer visibility, multi-user `autogroup:self` peer visibility, MagicDNS enabled/default-domain/custom-domain/disabled behavior, dual-stack/IPv4-only/IPv6-only prefix-family allocation, production-process IPv4-to-dual-stack prefix backfill after restart, dual-stack-to-IPv4-only and dual-stack-to-IPv6-only prefix-family removal backfill after restart, advertised-route, approved-route, primary-route, primary-failover, sticky primary return, primary-withdrawal, exit-node, current-head route-via/reload/restart/multiprefix/multiprefix-restart, current-head route-health failover/recovery/policy-reload/restart/all-unavailable/mixed-exit cases, production OIDC, restart persistence, DNS edge/IPv6-only MagicDNS, private DERP sidecar/STUN, Tailscale SSH allow/deny/timeout/group/no-policy/check-period, and current-head SSH localpart/profile scenarios against both implementations. Still port upstream stock-client scenarios for API auth, CLI over gRPC, exact SSH status/stderr snapshots, and the remaining ACL/DNS/route edge matrices. |

## Remaining Gap Backlog

Use this backlog to turn each `Partial` row into implementation and
parity-test work. Most upstream references are from the pinned v0.28.0
module under `tools/parity/headscale-go`; rows marked `upstream-main`
come from the 2026-05-22 sweep against `headscale/headscale` main
`4483fd0cad38717913e7509fc50f9d48c691b02b`. Local references are current
repo evidence, not claims about uninspected code.

| Priority | Gap | Upstream evidence | Local status | Actionable next step |
| --- | --- | --- | --- | --- |
| P0 | upstream-main gRPC/auth API drift | `proto/headscale/v1/headscale.proto` on upstream main adds the auth approval flow beyond the pinned v0.28 RPC set | Rust currently implements the v0.28-shaped `HeadscaleService` slices for users, nodes, preauth keys, API keys, policy, and health plus upstream-main `CheckPolicy`, `ExpireNode.disable_expiry`, and a compatibility `auth.proto` surface for `AuthRegister`/`AuthApprove`/`AuthReject` over the existing v0.28 registration cache | Finish the full upstream auth-request model, non-node auth flows, CLI `auth`, exact grpc-gateway error text, and real client approval coverage before claiming current-upstream auth parity |
| P0 | upstream-main policy model drift | Upstream main policy v2 includes grants, policy tests, SSH tests, nodeAttrs, randomize-client-port behavior, and SSH `localpart:*@domain` login users | Rust now parses top-level `randomizeClientPort` and emits `randomize-client-port` in `MapNode.CapMap`, parses/validates upstream-shaped `tests` and `sshTests`, semantically evaluates ACL `tests` plus `sshTests` for `CheckPolicy`/`SetPolicy`, accepts public `grants`/`nodeAttrs`/`ipsets`, projects network grants into ACL/filter behavior, validates capability/via/ipset/ipPool constraints, applies `grants[].via` to per-viewer route selection, and covers canonical SSH `localpart:*@domain` login-user semantics through compiler/checker and paired current-head stock-client smokes | Keep policy v2 differential fixtures growing as the executable baseline moves beyond v0.28, and broaden SSH profile/error variants |
| P0 | upstream-main CLI command drift | Upstream main exposes additional top-level commands including `serve`, `health`, `version`, `configtest`, `debug`, `generate private-key`, `mockoidc`, and `auth` | Rust has server/node/identity plus admin wrappers; user, node, API-key, preauth-key, policy, health, and debug-create-node paths use upstream gRPC connection modes, and `serve`, `version`, `completion`, `configtest`, hidden `dumpConfig`, `generate private-key`, and `mockoidc` are wired. Exact `auth`, current-upstream completion text, and current-upstream output/error text remain open | Add command/help snapshots against the selected upstream baseline, then implement missing top-level command aliases/behavior while keeping Octra-only commands out of upstream parity claims |
| P0 | drop-in database import and migration parity | Upstream uses its own database versioning/import expectations and supports existing headscale-go deployments beyond SQLite row-shape compatibility | Rust row shapes cover users, nodes, policies, preauth keys, API keys, and route migration slices; `headscale-db` now gates SQLite imports to the pinned v0.28-compatible window by accepting v0.28 `database_versions` rows or v0.28-complete Go migration history, preserving existing Go-shaped fixture rows, validating modern bcrypt-prefixed auth/API-key imports, preserving linked auth-key node metadata, and rejecting older/future Go markers before running Rust migrations. Postgres/GORM compatibility, Postgres runtime import, and future migration-version policy remain open | Decide whether Postgres import/runtime parity is required, and define future `database_versions` write behavior once the baseline moves past v0.28 |
| P0 | Noise-protected `/machine/*` endpoints | headscale-go serves map/register inside the TS2021/Noise path, validates the Noise machine key before map/register handling, and exposes only selected public exceptions such as `HEAD /machine/ping-response` | Rust now hard-gates public outer-router `/machine/*` fallback paths to empty 404, explicitly allows public `HEAD /machine/ping-response`, requires the Noise peer machine key for inner map/register routes, wraps existing-node register machine-key mismatches in HTTP 200 `RegisterResponse.Error`, and matches upstream `/map` 404 plain-text bodies for missing nodes and machine-key mismatches; keyed and flat missing-key cases are covered | Add inner-only coverage for the remaining `/machine/*` control endpoints and paired real-client replay/mismatch tests against the upstream v0.28 auth table |
| P0 | Node address allocation parity | headscale-go allocates IPv4 and IPv6 addresses from DB-aware used-address sets and supports prefix change/backfill semantics | Rust keeps Octra's existing `IpAllocator` contract while production `headscale server` now uses a stateful CIDR allocator seeded from persisted `nodes.ipv4`/`nodes.ipv6`, skips network/end and Tailscale-reserved service ranges, supports upstream `prefixes.allocation` values (`sequential`/`random`), carries optional IPv4/IPv6 through live `MachineRecord`, register, map `Addresses`/base `AllowedIPs`, policy/SSH node inputs, MagicDNS A/AAAA records, persistent SQLite hydration/write-through, CLI `prefixes.v6` parsing, upstream IPv6-only `prefixes.v6` config that disables the default IPv4 prefix, missing-family backfill assignment, configured family-removal backfill for IPv4 and IPv6, live-registry sync after backfill mutations, upstream-style `DebugCreateNode` pending nodes with no preallocated addresses, register-time address allocation for new CLI/web approvals including IPv6-only allocator coverage, reauth preservation before allocation, app-level duplicate IPv4/IPv6 rejection for direct admin-created nodes, and DB-level partial unique indexes for non-empty `nodes.ipv4`/`nodes.ipv6`. `tools/real-client/prefix-family-dual-stack-smoke.sh` / `tools/real-client/prefix-family-dual-stack-headscale-go-smoke.sh`, `tools/real-client/prefix-family-ipv4-only-smoke.sh` / `tools/real-client/prefix-family-ipv4-only-headscale-go-smoke.sh`, `tools/real-client/prefix-family-ipv6-only-smoke.sh` / `tools/real-client/prefix-family-ipv6-only-headscale-go-smoke.sh`, `tools/real-client/prefix-family-v4-to-dual-backfill-smoke.sh` / `tools/real-client/prefix-family-v4-to-dual-backfill-headscale-go-smoke.sh`, `tools/real-client/prefix-family-dual-stack-to-ipv4-only-backfill-smoke.sh` / `tools/real-client/prefix-family-dual-stack-to-ipv4-only-backfill-headscale-go-smoke.sh`, and `tools/real-client/prefix-family-dual-stack-to-ipv6-only-backfill-smoke.sh` / `tools/real-client/prefix-family-dual-stack-to-ipv6-only-backfill-headscale-go-smoke.sh` now provide paired stock-client coverage for dual-stack, IPv4-only, IPv6-only, production-process IPv4-to-dual-stack add backfill, and production-process family-removal backfill client-observed addresses against both implementations. Focused persistent-admin and gRPC tests cover family-change backfill for existing IPv4-to-dual, IPv4+IPv6-to-IPv6-only, and IPv6-to-dual cases. Remaining gaps are broader policy matching where upstream treats multiple node addresses as first-class | Broaden policy/SSH/DNS route matrices around multi-address nodes and keep Octra-facing structured allocation context cleanup on the consumer side |
| P0 | Persisted OIDC callback completion and node handoff | `hscontrol/oidc.go` completes `/oidc/callback` by validating state/nonce, merging UserInfo, creating/updating the user, then calling `handleRegistration` with OIDC expiry/register method | `headscale-api/src/oidc.rs::handle_callback` validates tokens/claims, can call `OidcUserStore`, renders success/expired-session responses, `tailscale_wire::router_with_oidc` completes the in-memory registration cache, `PersistentOidcRegistrationHandler` can write/rekey Go-shaped `nodes` rows, validate requested tags, and replace the old live-registry node key, the OIDC-configured CLI server path now opens/migrates SQLite, loads persisted ACL policy, and wires persistent users/preauth/nodes plus that handler into `ServeConfig::oidc`, `headscale-api/tests/oidc_callback_handoff.rs` proves mocked token/JWKS/UserInfo callback wakes the blocked wire follow-up, and paired `tools/real-client/oidc-smoke.sh`/`oidc-headscale-go-smoke.sh` prove the production callback flow with stock Tailscale clients | Extend the paired OIDC smokes into persisted policy/route mutation coverage, then wire those changes through the full multi-listener serving topology |
| P0 | Persisted-node wire runtime | headscale-go auth, map, routes, and gRPC all operate on persisted users/nodes/preauth keys and the state NodeStore | Rust admin/gRPC can use Go-shaped SQLite rows, and the OIDC-configured wire runtime now loads persisted ACL policy, strictly hydrates persisted `nodes` into the live registry on startup, writes auth-key registrations through persistent `nodes` while preserving `auth_key_id`, syncs map-time node runtime state back into `nodes` for restart hydration, can complete persistent web/CLI `RegisterNode` approvals through `MachineAdmin::complete_registration` with optional live-registry projection via `with_wire_registry`, and mounts the public grpc-gateway plus local Unix-socket and optional remote TCP gRPC from the same persistent stores in production `headscale server`. File-backed restart tests now cover auth-key, OIDC-handler, and web/CLI registration preserving node ID, user/auth-key FKs, routes/tags, disco key, endpoints, PreferredDERP, and SSH host keys through DB reopen and registry hydration, and paired restart-persistence stock-client smokes prove production Rust/headscale-go restart behavior for auth-key flows plus route/tag netmap churn; broader web/CLI/OIDC restart-runtime smokes are still partial | Broaden persistent web/CLI and OIDC registration stock-client smokes in the production serving topology; add broader restart/reload tests for map visibility and runtime mutations after auth-key and interactive registrations |
| P0 | Runtime task and NodeStore parity | headscale-go has a canonical State/NodeStore, map batcher, scheduled expiry, ephemeral cleanup, DERP refresh, extra-record watchers, SIGHUP policy reload tasks, and route-health probes | Rust has a live `MachineRegistry` projection plus persistent hydration/write-through slices, upstream-shaped ephemeral timers, scheduled node-expiry wakeups, config-backed DERP URL/path refresh, trusted-proxy forwarding-header gating on public listeners, production SIGHUP policy reload with route auto-approval fan-out, and HA-only route-health probes driven by `node.routes.ha` timing that update primary-route election. Persistent admin/gRPC node listing projects live online state and `LastSeen`, and paired online/restart stock-client smokes cover disconnect and auth-key restart behavior. Remaining runtime gaps are canonical NodeStore batching/reason parity and broader churn/restart map-stream tests | Make the persistent NodeStore/runtime state canonical and add churn/restart map-stream tests for each state-change reason |
| P1 | Current-upstream config schema and node-expiry config | Current upstream config includes additional blocks and defaults beyond the pinned v0.28 executable baseline, including trusted proxies, update checks, logtail, auto-update, node expiry, route HA timing, tuning, and database subfields | Rust now matches major v0.28 serving/DNS/OIDC/DERP/TLS/CLI fields, rejects removed keys, matches `log.format = "text"` by default, parses a pinned v0.28 config-example-derived fixture including `randomize_client_port` and octal Unix-socket permissions, parses and validates `trusted_proxies` and uses it to gate forwarding headers, parses `node.expiry` plus `HEADSCALE_NODE_EXPIRY`, accepts and validates `node.routes.ha` probe timing, projects node lifecycle fields into `/debug/config`, and drives non-tagged auth-key, interactive/web, and OIDC pending expiry from the same node-default semantics while keeping tagged registrations non-expiring. It also parses/projects update-check, logtail, auto-update, tuning, and database subfield surfaces where runtime supports them | Add configtest rows for remaining config blocks, `/debug/config` projection tests for config-backed vs default-filled fields, and exact ACME/Postgres fatal-text comparisons |
| P1 | Current-upstream route steering and route health | Current upstream policy grants can steer a route through a selected router via `via`, and route health participates in primary-route election | Rust derives served peer routes from each node's active approved routes, applies per-viewer `grants[].via` route selection so two routers advertising the same prefix can be steered to different viewers, rebuilds streams on policy changes and route-health failovers that move route `AllowedIPs`, covers v0.28-era primary route behavior, limits route-health ping scheduling to HA prefixes with 2+ active online subnet routers, runs probe-driven route-health transitions that fail over unhealthy primary candidates while preserving sticky recovery semantics, has unit coverage that policy-file reload recomputes `grants[].via` route ownership, and now has paired stock-client current-head smokes for two-viewer, policy-reloaded, production-restarted, multi-prefix, and production-restarted multi-prefix `grants[].via` route ownership, pause-driven route-health failover/recovery, policy-reloaded route-health failover, production-restarted route-health failover, all-unavailable route-health degraded-primary retention, and mixed exit-node/subnet-router route-health separation | Broaden route-via/route-health stock-client edge matrices for combinations beyond the covered basics |
| P1 | User CRUD policy/map refresh effects | Current upstream refreshes policy manager state around user lifecycle changes so maps and packet filters see user reference changes promptly | Rust gRPC and admin user create/delete/rename now call `PolicyStore::refresh()` after successful mutations so connected `/map` streams rebuild policy-derived state. Tests cover gRPC user CRUD and admin API user create/delete refreshing connected map streams with loaded policies | Broaden OIDC/web user lifecycle coverage and exact upstream error text around FK and rename edge cases |
| P1 | Config-driven serving topology | `hscontrol/app.go` starts public HTTP, grpc-gateway over a Unix gRPC socket, optional external gRPC with auth/TLS or insecure mode, reflection, metrics/debug, DNS, and DERP/STUN from one loaded config | Rust has individual routers/services and helper mount tests, and `headscale server` now starts the headscale-compatible wire path with or without OIDC, public `/api/v1` grpc-gateway routes, local Unix-socket gRPC/reflection, optional remote TCP gRPC/reflection, config-backed DNS injection into map responses, config-backed metrics/debug listener separation through `metrics_listen_addr`, startup DERP URL fetching, static local DERP path-map loading from upstream `derp:`, periodic DERP URL/path auto-update with live map-stream wakeups, config-backed embedded DERP/STUN backed by the same persistent stores, loaded-config `/debug/config` snapshots, trusted-proxy forwarding-header gating, SIGHUP ACL policy reload, and manual TLS cert/key material for remote gRPC plus the raw TLS control listener on upstream `listen_addr` when `https_listen` is absent. ACME HTTP-01/TLS-ALPN-01 issuance remains open; unsupported errors now distinguish HTTP-01's challenge listener from TLS-ALPN's public TLS listener and include the cache directory. Native DERP relay remains a sidecar-vs-native product decision | Build the remaining upstream topology from config: ACME issuance/cache handling and either native DERP relay or continued documented sidecar parity |
| P1 | gRPC/gateway exactness | `proto/headscale/v1/headscale.proto` exposes User, PreAuthKey, Node, ApiKey, Policy, and Health RPCs only; route approval is `SetApprovedRoutes` and version is public HTTP `/version` | Rust proto/service/gateway cover these slices; remaining doc/code comments about route/debug/version RPC expansion are stale for v0.28 | Do not add non-v0.28 RPCs for parity; instead finish listener wiring, automatic reflection mounting, grpc-gateway query/body parser edge cases, auth ordering, and status/error JSON text comparisons |
| P1 | Tailscale SSH real-client behavior | `integration/ssh_test.go` runs actual `tailscale ssh` allow/deny checks for groups, `autogroup:self`, no-policy, check-period, and cross-user denial; current-upstream policy handling also supports login-user patterns such as `localpart:*@domain` | Rust compiles/validates SSH policy, rejects circular SSH source-group references, expands nested source groups with recursion guards, emits `MapResponse.SSHPolicy`, fans out peer `sshHostKeys`, covers canonical `localpart:*@domain` login-user behavior in the compiler/checker, and paired OpenSSH-enabled stock-client scripts now assert successful hostname output, permission-denied, packet-filter timeout, flat group matrices, no-policy denial, `check`/check-period, and current-head localpart/profile cases against headscale-go | Add exact client-facing stderr/status text snapshots and broader profile-variant coverage |
| P1 | Embedded DERP/STUN/private DERP | headscale-go mounts `/derp`, `/derp/probe`, `/derp/latency-check`, `/bootstrap-dns`, optional client verification, embedded-region generation, and STUN serving | Rust covers verify/probe/latency/bootstrap/debug DERP-map shapes, DERPMap JSON fields, top-level `derp:` parsing, startup JSON URL map fetching, static local YAML path-map loading with merge/remove semantics, periodic URL/path auto-update, config-backed embedded-region generation, a native UDP STUN responder, and supervised upstream `derper` sidecar startup; top-level upstream `derp.server.enabled` maps to safe STUN-only embedded-region advertisement unless the Rust sidecar relay is explicitly configured. Paired private-DERP client coverage now passes through the supported upstream `derper` sidecar, including relay connectivity, STUN, verify-client admission, generated metadata, IPv6/userinfo-safe server URL derivation, and TLS interactions. Native Rust DERP relay protocol handling is not implemented or claimed; it remains a deliberate product decision beyond the supported sidecar boundary | Decide whether native DERP relay is required beyond the supported sidecar boundary; if not, keep sidecar parity documented and add only missing ACME/TLS/client edge cases |
| P2 | DNS/MagicDNS edge behavior | upstream DNS integration covers split DNS, resolver routes, extra records, base-domain validation, disabled DNS, and client-observed names | Rust has JSON coverage, production config-to-map DNS wiring, `override_local_dns` resolver placement, initial `extra_records_path` load/failure behavior, paired default/custom/disabled MagicDNS smokes, paired `tools/real-client/extra-records-smoke.sh` / `tools/real-client/extra-records-headscale-go-smoke.sh` coverage for client-observed operator A records, paired `dns-hot-reload` production smokes for `extra_records_path` mtime reload into stock-client netmaps, and paired DNS-edge/IPv6-only smokes for split resolver routes, AAAA/CNAME records, resolver metadata, and IPv6-only MagicDNS | Add upstream-vs-Rust MagicDNS `ExtraRecords` golden diffs, `CertDomains`/HTTPS behavior, and broader live resolver client behavior |
| P2 | Route edge transitions | upstream route tests cover auto-approval preservation, primary owner selection, failover, withdrawals, exit routes, offline/expired nodes, and policy-triggered updates | Rust covers major real-client route flows, differential auto-approval matrices, approval preservation across withdrawal/reauth, explicit unapproval, policy reload preserving approvals, simultaneous candidates, offline/expired/ephemeral router removal, withdraw map-delta shape, route-health failover map-stream deltas, and paired stock-client current-head route-via/route-health basics including multi-prefix route-via steering, policy-reloaded route-health failover, all-unavailable degraded-primary retention, and mixed exit-node/subnet-router HA separation | Add broader paired stock-client route edge coverage for reload/restart route interactions |
| P2 | Ping lifecycle | Upstream main mounts public `HEAD /machine/ping-response` and wires `PingRequest` callbacks through the state ping tracker; the pinned v0.28 baseline does not carry this executable slice | Rust now exposes the public HEAD callback, keeps adjacent public `/machine/*` paths 404, registers unguessable ping IDs, dispatches URL callback `PingRequest` chunks on active map streams, and correlates successful callbacks back to the pending ping waiter | Add the operator/debug UI ping initiation surfaces, paired stock-client ping lifecycle smoke coverage, and later non-HEAD PingRequest types (`TSMP`/`disco`/`c2n`) if replacement parity requires them |
| P2 | CLI over upstream gRPC | headscale-go CLI connects via config, Unix socket by default, remote gRPC with API key/TLS, and exposes `serve`, `version`, `completion`, `configtest`, `dumpConfig`, `debug create-node`, users, nodes, preauthkeys, apikeys, and policy commands | Rust CLI has HTTP-admin wrappers, aliases, and output formats for several groups, plus extra Octra-era commands; user create/list/rename/destroy, API-key create/list/expire/delete, preauth-key create/list/expire/delete, policy get/set/check, top-level health, `debug create-node`, `version`, `completion`, hidden `dumpConfig`, `generate private-key`, `mockoidc`, and `configtest` now run with upstream config discovery, upstream top-level server/config aliases for the production SQLite path and core listeners, `cli.address`/`cli.api_key`/`cli.insecure` config merging, upstream `--insecure` TLS semantics, selectors, top-level and migrated-admin help snapshots, upstream-style raw first-arg config-load exceptions for `version`/`mockoidc`/`completion`, `completion --no-descriptions`, and legacy `--server` fallback only when explicitly requested. Exact Cobra byte-for-byte help/script output remains open | Add help/output/error snapshots against pinned headscale-go, free the upstream `node` alias by moving Octra mesh-node behavior out of the parity CLI, broaden TLS/ACME/DERP/metrics/policy server-init validation, and keep Octra-only commands out of parity claims |
| P2 | API auth and error text | upstream grpc-gateway and gRPC auth return specific unauthenticated/status bodies and parser errors before handlers run | Rust covers bearer auth and Go base-0 uint64 parsing, with remaining exact query/body/error cases open | Add table-driven gateway and gRPC tests for missing/invalid bearer, malformed query/body JSON, path parser failures, status JSON fields, and CLI-visible error text |
| P2 | Octra consumer boundary | Octra consumes headscale-rs through `headscale-api`, ACL, the `PreauthRedeemer`/`IpAllocator` traits, and the embedded CLI, while headscale-rs should remain a generic headscale-go replacement | Static Octra inspection found full-Hub admin-router documentation drift, split preauth stores in `mesh serve`, stale embedded CLI docs, and Octra-specific policy terminology/tests that should not define upstream compatibility | Keep generic contract fixes in headscale-rs, and move Octra-specific admin mounting, preauth store unification, embedded CLI docs, and settlement/billing policy behavior into Octra-side tests and code |

## Parallel Workstreams

These streams are independent enough to run in separate worktrees. Keep
file ownership narrow when splitting them across agents.

### Active Task Board

| Lane | Status | Owner scope | Next handoff |
| --- | --- | --- | --- |
| CLI node/debug/health gRPC client plumbing | Done | `headscale-cli/src/admin/grpc_client.rs` | Client helpers exist for node, debug-create-node, backfill, and health RPCs |
| CI/fuzz/coverage gates | Done | `.github/workflows/**`, `scripts/**` | CI now runs all-target workspace/standalone tests, checks checked-in fuzz corpus directories, keeps the 10k fuzz lane live, and enforces the coverage floor before heavier real-client gates |
| CLI nodes command migration | Done | `headscale-cli/src/admin/nodes.rs`, `headscale-cli/src/admin/mod.rs`, focused CLI tests | `headscale nodes` defaults to upstream gRPC with explicit `--server` legacy HTTP fallback where supported |
| CLI config and snapshots | In flight | `headscale-cli/src/main.rs`, `headscale-cli/src/config.rs`, CLI snapshot tests | `cli.address`/`cli.api_key`/`cli.insecure` plus top-level `unix_socket`, upstream default config discovery, upstream top-level server/config alias mapping, JSON config parsing, top-level current-upstream `auth`, `serve`, `health`, `debug create-node`, `version`, `completion`, `completion --no-descriptions`, raw first-argument config-load exceptions, `generate private-key`, `mockoidc`, hidden `dumpConfig`, top-level/migrated-admin/auth help snapshots, listener-free `configtest` validation, and unsupported Postgres `serve` failure are wired and covered by process-level smoke tests; add byte-for-byte upstream help/output/error snapshots and broader TLS/ACME/DERP/metrics/policy server-init validation next |
| Wire auth boundary | Ready next | `headscale-api/src/tailscale_wire/**`, `headscale-cli/src/server.rs`, paired wire tests | Gate public `/machine/*`, require Noise peer machine-key context for map/register, and add upstream mismatch tests before expanding more runtime features |
| Address allocation | In flight | `headscale-api/src/tailscale_wire/**`, `headscale-db/**`, Octra adapter tests | Production allocation is now DB-seeded and config-aware for v4/v6, debug/CLI registration allocates at approval time like upstream, direct admin creation rejects duplicate addresses, DB partial unique indexes protect non-empty manual node addresses, `MachineRecord` tolerates IPv4-disabled nodes, upstream IPv6-only prefix config disables the default IPv4 prefix, paired dual-stack/IPv4-only/IPv6-only prefix-family stock-client smokes cover initial configured family allocation cases, paired production-process IPv4-to-dual-stack and dual-stack-to-single-family stock-client smokes cover restart plus `nodes backfillips`, and persistent-admin/gRPC tests cover prefix-family add/remove backfill semantics; broaden multi-address policy/SSH/DNS/route coverage plus Octra-facing structured allocation context cleanup |
| Runtime/auth parity | Ready next | `headscale-api/proto/auth.proto`, `headscale-api/src/grpc.rs`, `headscale-api/src/grpc_gateway.rs`, wire registration tests | Decide baseline before expanding beyond the pinned v0.28 registration cache into current-upstream auth-request semantics |
| Persistent runtime smokes | In flight | `tools/real-client/**`, server/runtime tests | Online/LastSeen and restart-persistence stock-client lifecycle are covered; broaden production-process restart/mutation smokes for web/CLI/OIDC registration and more map-stream churn cases |
| Octra boundary checks | Ready next | `/Users/androolloyd/Development/octra/crates/**`, headscale trait tests | Keep full-Hub admin routing, preauth store unification, embedded CLI docs, and Octra policy terminology on the Octra side unless the generic headscale contract is missing |
| DB import matrix | Done for SQLite window | `headscale-db/tests/**`, fixture DBs, migration guard | SQLite import fixtures now cover v0.26.0, v0.27.1, v0.28.0-beta.1, v0.28.0-beta.2, and v0.28.0 with explicit accepted/rejected windows; Postgres remains an unsupported runtime/import boundary |

| Stream | Scope | Primary files | Exit criteria |
| --- | --- | --- | --- |
| Control-plane topology | Add optional remote TCP gRPC serving with API-key auth, TLS/insecure config, reflection, and metrics/debug listener separation; keep the local Unix socket unauthenticated and permission-protected like upstream. | `headscale-cli/src/server.rs`, `headscale-cli/src/config.rs`, `headscale-cli/src/main.rs`, `headscale-api/src/grpc.rs`, `headscale-api/src/tailscale_wire/serve.rs` | Remote helper tests prove API-key-authenticated `Health` plus reflection, config parses upstream-shaped gRPC and metrics fields, local Unix health remains bearerless, and production serving no longer mounts metrics/debug on the public control router; still add CLI-over-gRPC and broader production-process smokes. |
| CLI transport parity | Move admin commands from legacy HTTP wrappers toward upstream gRPC connection modes: Unix socket by default, remote address with API key/TLS/insecure, and upstream output/error snapshots. | `headscale-cli/src/admin/**`, `headscale-cli/tests/**`, `headscale-cli/src/config.rs` | CLI tests prove Unix-socket gRPC users, nodes, API-key, preauth-key, policy, health, debug-create-node, and mock OIDC commands against production-style services, and process tests cover the top-level command surface. Remaining exit criteria are upstream byte-for-byte output/error snapshots, broader server-init validation, and broader process-level config tests while preserving Octra-only commands outside parity claims. |
| Runtime DNS and DERP config | Harden the loaded-config DNS/DERP runtime now that production maps use configured `DnsStore` and embedded DERP/STUN settings. | `headscale-cli/src/server.rs`, `headscale-cli/src/config.rs`, `headscale-api/src/dns.rs`, `headscale-api/src/tailscale_wire/**`, `tools/real-client/**` | Full map responses from production include configured DERP and MagicDNS, `override_local_dns` is honored in the tailcfg DNS shape, invalid initial `extra_records_path` files fail before serving, and paired rows now cover split DNS, DNS resolver metadata, IPv6-only MagicDNS, production `extra_records_path` hot reload, and private DERP sidecar/STUN. Remaining exit criteria are CertDomains/HTTPS edges and route-health runtime integration. |
| Embedded DERP/STUN | Harden the config-backed embedded DERP/STUN runtime: native STUN, DERP map generation, `derper` sidecar lifecycle, verify-client admission, and TLS/server-url behavior. | `headscale-core/src/derp.rs`, `headscale-core/src/stun.rs`, `headscale-cli/src/server.rs`, `headscale-cli/src/config.rs`, `tools/real-client/**` | Paired `derp-private` real-client rows pass against Rust and pinned headscale-go through the supported upstream `derper` sidecar with STUN and verify-client admission; native DERP relay remains a deliberate open decision. |
| Lifecycle and route correctness | Preserve route approvals across reauth even when not currently advertised and tighten map-change batching/reason coverage. | `headscale-api/src/tailscale_wire/register.rs`, `headscale-api/src/tailscale_wire/map.rs`, `headscale-api/src/tailscale_wire/mod.rs`, `headscale-cli/src/server.rs` | Tests prove absent-route reauth keeps DB approvals, expired/ephemeral nodes disappear from peers, stream-session online/offline state uses headscale-go's disconnect grace, production ephemeral GC uses per-node timers with persistent deletion, scheduled expiry wakeups preserve node rows while waking streams, and streaming deltas match upstream-visible behavior. |
| CI, fuzz, and coverage gates | Enforce stronger parity in CI: `--all-targets` tests, selected real-client rows, stale-corpus checks, coverage thresholds, and an explicit formal-verification status. | `.github/workflows/**`, `scripts/**`, `headscale-core/fuzz/**`, `tools/real-client/**` | PR CI runs 10k fuzz plus stale-corpus check, selected Docker smokes, all-target tests, and coverage floor; docs state Lean/formal proof is absent until restored. |

## Next Implementation Order

1. Continue CLI transport parity on top of the users/nodes/API-key/preauth/policy
   gRPC slice: add upstream help/output/error snapshots, exact
   gateway/parser error text, and remaining auth output/error behavior.
2. Broaden paired stock-client route reload/restart coverage on top of the
   current-head route-via, route-via-reload, route-via-restart,
   route-via-multiprefix, route-health, route-health-reload,
   route-health-restart, route-health-all-unhealthy, and
   route-health-mixed-exit smokes.
3. Finish config-runtime gaps: ACME HTTP-01/TLS-ALPN issuance behavior,
   SIGHUP/config reload, and explicit Postgres unsupported/runtime
   messaging.
4. Broaden production-process restart smokes for web/CLI/OIDC
   registration and map churn beyond the auth-key restart-persistence
   row.
5. Decide whether native Rust DERP relay is required beyond the
   supported upstream `derper` sidecar boundary; do not claim native
   relay parity unless that product decision changes.
6. Extend CI/fuzz/golden enforcement: selected real-client rows on PRs,
   scheduled full matrix, parity golden diffs, and an explicit
   formal-verification gate once the Lean workspace exists again.
