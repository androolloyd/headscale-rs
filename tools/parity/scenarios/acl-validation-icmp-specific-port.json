{
  "name": "acl-validation-icmp-specific-port",
  "expect_policy_error": "protocol \"icmp\" does not support specific ports",
  "policy": {
    "acls": [
      {
        "action": "accept",
        "proto": "icmp",
        "src": ["*"],
        "dst": ["*:80"]
      }
    ]
  }
}
