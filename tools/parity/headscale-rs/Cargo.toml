[package]
name = "headscale-rs-parity"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
anyhow = "1"
headscale-api = { path = "../../../headscale-api", default-features = false }
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[workspace]
